version: "3" # composeファイルのバージョン
services: 
    app: # サービス名
        build:
            context: .
            dockerfile: ./docker/go/Dockerfile # ①ビルドに使うDockerfileの場所
        tty: true # ②コンテナの永続化
        volumes:
            - ./src/app:/go/src/app # ③マウントディレクトリ